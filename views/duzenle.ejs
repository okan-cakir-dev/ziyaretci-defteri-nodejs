<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <title>Mesajı Düzenle</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container mt-5">
        <div class="card p-4 shadow-sm">
            <h3 class="text-center">Mesajı Düzenle ✏️</h3>
            
            <form action="/guncelle/<%= mesaj._id %>" method="POST">
                <div class="mb-3">
                    <label>İsim:</label>
                    <input type="text" name="isim" class="form-control" value="<%= mesaj.isim %>" required>
                </div>
                <div class="mb-3">
                    <label>Mesaj:</label>
                    <textarea name="not" class="form-control" rows="3" required><%= mesaj.not %></textarea>
                </div>
                <button type="submit" class="btn btn-success w-100">Güncelle</button>
                <a href="/" class="btn btn-secondary w-100 mt-2">İptal</a>
            </form>
        </div>
    </div>
</body>
</html>